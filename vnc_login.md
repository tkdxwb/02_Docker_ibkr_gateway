# 通过 SSH 隧道连接 VNC

## 端口对应关系

| 账户 | 容器 | 服务器本地端口 |
|------|------|---------------|
| 账户1 | ib-gateway-1 | 5900 |
| 账户2 | ib-gateway-2 | 5901 |
| 账户3 | ib-gateway-3 | 5902 |

## 建立隧道

在**本地机器**执行以下命令，同时转发三个账户的 VNC 端口：

```bash
ssh -L 5900:127.0.0.1:15900 \
    -L 5901:127.0.0.1:15901 \
    -L 5902:127.0.0.1:15902 \
    yue@ubuntu-home.tailfa4a66.ts.net -N
```

连接则是
```bash
open vnc://localhost:15900
```
> `-N` 表示不执行远程命令，仅保持隧道。命令阻塞运行期间隧道持续有效，`Ctrl+C` 断开。

## 连接 VNC

隧道建立后，用任意 VNC 客户端连接本地地址：

```
账户1: localhost:5900
账户2: localhost:5901
账户3: localhost:5902
```

密码：`.env` 文件中的 `VNC_SERVER_PASSWORD`。

## 验证服务器端口（可选）

在服务器上确认容器 VNC 端口已监听：

```bash
ss -tlnp | grep 590
```

应看到三行 `127.0.0.1:5900/5901/5902`。
